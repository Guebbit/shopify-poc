/*
 * vue-i18n runtime options, loaded by @nuxtjs/i18n (module convention path).
 * Explicit fallback: a key missing from a locale renders the English message
 * instead of the raw key path.
 */
export default defineI18nConfig(() => ({
    fallbackLocale: 'en'
}));
